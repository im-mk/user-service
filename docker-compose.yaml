services:
  user-service:
    container_name: user-service
    image: user-service
    environment:
      DB_HOST: user-service-db
    ports:
      - "8080:8080"
    networks:
      - network1
    depends_on:
      postgres:
        condition: service_healthy
      liquibase:
        condition: service_completed_successfully

  postgres:
    container_name: user-service-db
    image: postgres
    volumes:
      - postgres:/data/postgres
      - ./database/init:/docker-entrypoint-initdb.d
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-postgres}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-postgres}
      PGDATA: /data/postgres
    ports:
      - "5432:5432"
    networks:
      - network1
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U $POSTGRES_USER -d $POSTGRES_DB"]
      interval: 10s
      timeout: 5s
      retries: 3

  liquibase:
    container_name: liquibase
    image: liquibase/liquibase
    command: --defaultsFile=liquibase.properties update
    volumes:
      - ./database/liquibase.properties:/liquibase/liquibase.properties
      - ./database/changelog.yaml:/liquibase/changelog/changelog.yaml
      - ./database/sql:/liquibase/changelog/sql
    environment:
      - LIQUIBASE_COMMAND_URL=jdbc:postgresql://user-service-db:5432/${POSTGRES_DATABASE:-userservicedb}
      - LIQUIBASE_COMMAND_USERNAME=${POSTGRES_USER:-postgres}
      - LIQUIBASE_COMMAND_PASSWORD=${POSTGRES_PASSWORD:-postgres}
    networks:
      - network1
    depends_on:
      postgres:
        condition: service_healthy

networks:
  network1:
    driver: bridge

volumes:
  postgres:
